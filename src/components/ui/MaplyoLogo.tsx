import React from "react";

export function MaplyoLogo({ className = "w-8 h-8", classNameText = "text-xl", showText = true }: { className?: string; classNameText?: string; showText?: boolean }) {
    return (
        stroke = { monochrome? "currentColor": "url(#brandGradient)" }
                strokeWidth = "6"
    strokeLinecap = "round"
    className = { monochrome? classNamePath: "" }
        />

        </svg >
    );
};
